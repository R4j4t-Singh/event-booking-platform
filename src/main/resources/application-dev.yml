server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/event_booking_platform
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
    level:
      com.example.event_booking_platform: debug

jwt:
  secret: supersecretkey12345678904545444589
  expiration: 3600000
